apiVersion: v1
kind: Service
metadata:
  name: {{ include "openclaw-chart.fullname" . }}
  labels:
    {{- include "openclaw-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: gateway
      port: {{ .Values.service.gatewayPort }}
      targetPort: gateway
      protocol: TCP
    - name: bridge
      port: {{ .Values.service.bridgePort }}
      targetPort: bridge
      protocol: TCP
  selector:
    {{- include "openclaw-chart.selectorLabels" . | nindent 4 }}
